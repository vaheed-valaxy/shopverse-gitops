apiVersion: v2
name: shopverse-backend
description: Helm chart for the Shopverse backend service
type: application
version: 1.0.0
appVersion: "1.0.0"

dependencies:
  - name: shopverse-common
    version: "1.0.0"
    repository: "file://../shopverse-common"
